on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: "Printing folder contents"
        run: ls

    

      - uses: actions/checkout@v3

      - name: "Login to Docker hub"
         
